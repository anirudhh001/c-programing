#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    int sum = 0;
    int num = N;
    if (num < 0) {
        num = -num;
    }
    while (num > 0) {
        sum += num % 10;
        num /= 10;
    }
    printf("%d", sum);
    return 0;
}


