#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    
    int x, y;
    int temp;
    
    scanf("%d %d", &x, &y);
    
    temp = x;
    x = y;
    y = temp;
    
    printf("%d %d", x, y);
    
    return 0;
}
